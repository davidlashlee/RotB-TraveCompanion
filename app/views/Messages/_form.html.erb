<%= form_for @message do |f| %>
  <p>
    <%= f.label :reciever_id %><br>
   <%= f.select :reciever_id, options_for_select(@user.collect{ |c| [c.email, c.id] }, f.object.reciever_id)%>
   </p>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
    <p>
    <%= f.label :message_text %><br>
    <%= f.text_area :message_text %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

